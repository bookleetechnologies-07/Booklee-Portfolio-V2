/**
 * Shared ids for the `Built around you.` handoff.
 *
 * The phrase exists exactly twice in the markup: once as real text inside the
 * ERP concept screen, and once inside the next section's heading where it
 * finally lives. The overlay that carries it between the two is a third node,
 * but it is `aria-hidden` and only visible while both of the others are not.
 */
export const PHRASE_SRC_ID = "phrase-source";
export const PHRASE_DEST_ID = "phrase-destination";
export const OFFERED_SECTION_ID = "websites-offered";
